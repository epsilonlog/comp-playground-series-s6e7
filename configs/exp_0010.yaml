# exp_0010 — immutable once run (2026-08-30)
exp_id: exp_0010
model: xgb
params: {}                 # XGBoost defaults + house params (hist, seed 42, n_jobs 6)
features: baseline
folds: data/processed/folds.parquet
decision_rule: argmax
parent: exp_0001
changed: "new family: XGBoost defaults, baseline features (diversity for the blend)"
result:
  cv_mean: 0.88042
  cv_std: 0.00194
  fit_mean: 0.90058
  runtime_s: 123
  notes: >
    Beats the tuned LGBM combo at argmax (0.88042 vs 0.87742) out of the box - deeper
    default trees (max_depth 6) suit this data. Brought in for error decorrelation,
    not for its level; the level difference dissolves after prior correction anyway.
