# exp_0013 — immutable once run (2026-08-31)
exp_id: exp_0013
model: ftt
params: {}          # builder defaults ARE the reproduction recipe — see src/s6e7/models/ftt.py
features: baseline
folds: data/processed/folds.parquet
decision_rule: argmax   # prior-corrected rule measured separately (run_rule), as exp_0015
parent: exp_0001
changed: "new family: FT-Transformer (masamlp, Kawamata recipe), raw features"
provenance: >
  Step-8 reproduction of "S6E7 | FT-Transformer-v2 [CV: 0.95063]" (masayakawamata),
  private LB 0.95084 — the base model of the 2nd- and 4th-place solutions. Libraries
  pinned to the notebook's own versions: masamlp==0.3.0, catstat==0.4.0. Config verbatim
  from the notebook except: our frozen 5 folds instead of its 7 (rule 6 — comparability
  with exp_0001..0012 beats matching its absolute number), and n_ens=1 instead of 4
  (its own paired measurement: +0.00017 BA, inside metric discreteness, at 4x wall
  clock). Trained on Kaggle GPU via notebooks/kaggle_ftt/ — local box is CPU-only torch.
result:
  cv_mean: 0.87159
  cv_std: 0.00120
  fold_scores: [0.87022, 0.87273, 0.87290, 0.87067, 0.87141]
  runtime_s: 1063          # Kaggle T4, 5 folds x 16 epochs
  per_class_recall: {at-risk: 0.993, fit: 0.827, unhealthy: 0.795}
  paired_vs_parent: {diff: -0.00132, t: -3.2, folds_positive: 0/5}
  error_corr_vs_exp_0007: 0.878   # the two GBDTs read 0.885 - no decorrelation from raw features
  notes: >
    Carried back 2026-09-03 from kernel aligh474/s6e7-ftt-exp-0013-0014 (run 2026-08-31).
    On the 13 raw columns the FT-Transformer is a slightly WORSE argmax model than
    LightGBM defaults (-0.0013, all five folds negative), and it fails on the same rows
    (error correlation 0.878 vs the 0.885 between the two GBDT families). The source
    notebook's 0.9506 was never the raw network - it was the network plus per-value
    target encoding, which is exp_0014's variable. Rule-corrected score: exp_0015.
