# exp_0001 — immutable once run (2026-08-30)
exp_id: exp_0001
model: lgbm
params: {}                 # LGBMClassifier defaults + house params (random_state=42, n_jobs=6, verbosity=-1)
features: baseline_matrix  # 7 numeric raw, 4 ordinal codes, 2 nominal codes; nulls = NaN
folds: data/processed/folds.parquet   # StratifiedKFold(5) on target, seed 42, frozen
decision_rule: argmax
parent: null
changed: "first baseline: LGBM defaults, raw features, argmax"
result:
  cv_mean: 0.87291
  cv_std: 0.00205
  fold_scores: [0.87121, 0.87512, 0.87503, 0.87089, 0.87228]
  runtime_s: 58
  per_class_recall: {at-risk: 0.993, fit: 0.825, unhealthy: 0.801}
  lb_public: 0.87210   # CV - 0.00081
  lb_private: 0.87171  # CV - 0.00120 (visible because the competition has ended)
  notes: >
    Reliability curves sit on the diagonal (well calibrated). Errors concentrate in
    fit->at-risk (0.172) and unhealthy->at-risk (0.198) - the training-prior effect the
    OOF multiplier search targets. That search is exp_0002.
